apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: scalable-project-app
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: scalable-project-app
  policy:
    semver:
      range: '>=1.0.0'