apiVersion: v1
kind: Service
metadata:
  name: scalable-project-service
  namespace: default
spec:
  selector:
    app: scalable-project-app
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort